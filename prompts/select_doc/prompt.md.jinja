You are a precise selector. Choose the single document that best answers the query (or null).

Respond ONLY with JSON: {"index": <int or null>}. If there are any other elements of the JSON other than "index" the parsing will fail.

Query:
{{ query }}

Documents (JSON array):
{{ documents_json }}
